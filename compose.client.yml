name: timed-ssh-client

services:
  ssh-client:
    build:
      context: ./ssh-client
    image: timed-ssh-client:latest
    container_name: timed-ssh-client
    environment:
      SSH_HOST: "${SSH_HOST:-ssh-server}"
      SSH_PORT: "${SSH_PORT:-22}"
      SSH_USER: "${SSH_USER:-deploy}"
      SSH_PRIVATE_KEY: /run/sshfling-secrets/client_ed25519
      SSH_KNOWN_HOSTS: "${SSH_KNOWN_HOSTS:-}"
      SSH_KNOWN_HOSTS_FILE: "${SSH_KNOWN_HOSTS_FILE:-}"
      SSH_HOST_KEY_SHA256: "${SSH_HOST_KEY_SHA256:-}"
      SSH_COMMAND: "${SSH_COMMAND:-whoami && hostname && date -u}"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 64
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=16m
    volumes:
      - type: bind
        source: ./secrets
        target: /run/sshfling-secrets
        read_only: true
    networks:
      - timed-ssh-net

networks:
  timed-ssh-net:
    external: true
