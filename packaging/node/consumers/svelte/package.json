{
  "name": "sshfling-svelte-server-consumer",
  "version": "0.0.0",
  "private": true,
  "description": "Server-rendered Svelte consumer of the SSHFling Node API.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run build && node test/render.mjs"
  },
  "dependencies": {
    "sshfling": "file:../..",
    "svelte": "5.35.2"
  },
  "engines": {
    "node": ">=18"
  }
}
